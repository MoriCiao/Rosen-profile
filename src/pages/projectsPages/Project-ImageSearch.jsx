import React from "react";

const ImageSearch = () => {
  return (
    <div>
      🔍 Image Search 圖片搜尋器 使用技術： React, JavaScript, HTML, CSS 簡介：
      這是一個基於 API
      的圖片搜尋工具，使用者可輸入關鍵字，立即取得相關圖片並顯示在畫面上。主要練習如何整合
      API、非同步請求與資料渲染。 特色功能：
      關鍵字搜尋圖片（推測應接上某公開圖片 API） 輕量前端實作。
    </div>
  );
};

export default ImageSearch;
